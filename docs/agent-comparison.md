# NovaClaw: сравнение 0.1.1 (форк) vs 0.1.5 (официальный)

## Участники

| | 0.1.1 | 0.1.5 |
|---|-------|------|
| Каталог | `/home/jinx/Disk2/novaclaw` | `/home/jinx/Disk2/NovaClaw-0.1_5-source/` |
| Тип | git-репозиторий (ветка `diagnosis-notes`) | дерево исходников, `.git` отсутствует |
| Версия | 0.1.1 | 0.1.5 |

## Масштаб различий

- **722 файла различаются** — файлы массово переименованы/переписаны
- **133 только в 0.1.1** — e2e/performance, старые темы, компоненты
- **236 только в 0.1.5** — новые структуры, переименования

## Ключевые изменения (по пакетам)

| Пакет   | Изменения                              |
|---------|----------------------------------------|
| `core`  | Контекстный бюджет, compact-оверлей, safe mode (~663 строк)|
| `novaclaw` | CLI: command-registry, lazy-command (~380 строк)  |
| `app`   | Настройки, terminate-view, session recovery (~377 строк)|

## Рекомендация: Вариант A — Взять 0.1.5 за новую базу целиком

**Почему?** Переносить дельты в 0.1.1 = ~1,000 правок с конфликтами по всему ядру.

### План применения (шаги):
```bash
# 1. Новая ветка
git checkout -b upgrade-015

# 2. Дробьт содержимое 0.1.5
rsync -a --exclude node_modules \
  /home/jinx/Disk2/NovaClaw-0.1_5-source/ .

# 3. Валидация и закоммит
bun install && bun run dev -- --help
git add . && git commit -m "deps: bump to NovaClaw 0.1.5 base"
```

---
*Generated by Agent A & B during upgrade diagnosis session.*
